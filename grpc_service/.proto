syntax = "proto3";

service ThingSpeakService {
  rpc SubscribeToChannel (Empty) returns (stream DataResponse);
}

message Empty {}

message DataResponse {
  string channel_id = 1;
  string field_data = 2;
  string timestamp = 3;
}

